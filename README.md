If you'd like to run it in your machine:

    $ git clone git://github.com/justhink/justhink.github.com.git
    $ cd justhink.github.com
    $ bundle
    $ jekyll --server

Then access in your browser [http://localhost:4000][4].
